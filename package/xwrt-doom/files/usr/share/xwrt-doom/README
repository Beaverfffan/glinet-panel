# xwrt-doom —— 机身小屏上的触屏 Doom

目标硬件：**GL.iNet GL-BE14000**
屏幕：320x240 `panel-mipi-dbi`（ST7789P3 系）@ SPI0 52 MHz
触摸：Hynitron CST353X 电容屏（**单点**），`/dev/input/event0`

实测：**320x240 RGB565 上屏 36.0 fps**（独占 DRM master 时逐秒稳定
`Δ = 5,529,996 B = 36 × 153,611 B`；这是 SPI 带宽上限，不是 CPU 上限）。

---

## 1. 先弄一份 IWAD

程序自己要一个 WAD 数据文件（游戏关卡/贴图/音效）。三种选择：

| 方式 | 命令 / 操作 | 说明 |
|---|---|---|
| shareware 版 | `xwrt-doom-get-wad` | 在路由器上跑，约 4 MB，Episode 1 全关。id 允许自由分发。 |
| 自备 | 把 `doom1.wad` 拷到 `/usr/share/xwrt-doom/` | 完整版三章请自备 `doom.wad` 并改用 `-iwad` |
| Freedoom | 把 `freedoom1.wad` 拷进去 | 完全自由（GPL），美术/关卡是重做的 |

放好后改 `/etc/config/xwrt_doom` 的 `wad` 指向实际文件即可。

校验一份 shareware doom1.wad 是否正确：

```
大小 4,196,020 字节    md5 f0cefca49926d00903cf57551d901abe
```

> ⚠️ `xwrt-doom-get-wad` 三个下载器一律带「不校验证书」。这不是偷懒：实测这台
> 机器的 TLS 信任链本身是坏的（`ca-bundle` 与 `libustream-mbedtls` 都在，但
> `uclient-fetch` 访问 github **和** downloads.openwrt.org 都报
> `certificate is self-signed or not signed by a trusted CA`），是**全局**问题
> 而不是 GitHub 特例。脚本用固定的「字节数 + md5」双重校验兜底。
> 如果 https 实在走不通，可以在 PC 上下好再拷过去。

## 2. 起停

```sh
uci set xwrt_doom.main.enabled='1'
uci commit xwrt_doom
/etc/init.d/xwrt-doom start          # 起
/etc/init.d/xwrt-doom stop           # 停（会自动把面板服务拉回来）
/etc/init.d/xwrt-doom restart
```

也可以直接在 **LuCI → 服务 → 屏幕上的 Doom** 里点。

**退出 Doom**：点控制条左下的 `MNU` 翻到第 2 页，点最右边的红色 `EXIT`。
原版 Doom 1.9 的菜单里没有 Quit Game，所以这个按钮是唯一的屏幕出口。

## 3. 怎么玩

屏幕底部 40 行是控制条（`view=reserve` 时），上面 200 行是 Doom 画面。

**第 1 页（游戏中）**

| 按钮 | 作用 |
|---|---|
| `MNU` | 翻到第 2 页（菜单 / 地图 / 退出） |
| `RUN` | **点一下锁定**，再点解开。锁定时底色是琥珀色 |
| `USE` | 开门 / 拉开关（按住生效） |
| `FIRE` | **点一下锁定**（连发），再点解开 |

**画面区手势**（单点屏的关键设计）

- **左半屏拖动** = 虚拟摇杆：前 / 后 / 左右平移。手指按下的那一点就是摇杆原点，
  推得超过半径时原点会跟着手指走（所以手指滑到边上也不会"顶住"）。
- **右半屏拖动** = 左右转向。
- **轻点画面**（按住 < 220 ms 且几乎没移动）= **开一枪**。这条是给单点屏补的：
  手指按着 `FIRE` 就没法同时滑动转向，所以轻点画面成了最顺手的开火方式。

**第 2 页（菜单）**

`ESC` `ENT` `Y` `N` `GAM`（亮度）`MAP`（大地图）`EXIT`（退出程序）

## 4. 调参

都在 `/etc/config/xwrt_doom`，改完 `/etc/init.d/xwrt-doom restart` 生效。

- `fps` —— 上屏帧率上限。**默认 0 = 不设上限**。
  ⚠️ 标定过的坑：引擎一帧等于一个 game tic（28.57 ms / 35 Hz），所以任何
  「距上次提交 >= 1/fps 才提交」的简单门都会和它**拍频**：`1/30 = 33.3 ms`
  只比 28.57 ms 大一点，于是每个提交都要等第 2 个 tic ⇒ 帧率腰斩到 17.5 fps。
  现在的实现是令牌桶（攒到 85% 预算就放行），目标 30 就是 30。
- `view` —— `reserve`（默认，控制条占底部 40 行）/ `stretch`（纵向 1.2 倍铺满，
  这才是当年 CRT 上 320x200 的正确比例，控制条半透明压在画面下缘）。
- `deadzone` / `radius` / `tap_ms` / `pulse_ms` —— 手感。
  `pulse_ms` 别调到 40 以下：Doom 读 `gamekeydown[]`，按下与释放落在同一个
  35 Hz tick 内等于没按过。
- `stop_panel` / `restore_panel` —— 与面板服务的互斥与恢复。
- `extra_args` —— 原样透传给引擎，例如 `-warp 1 1`（直接进 E1M1）、
  `-skill 4`、`-timedemo demo1`。

## 5. 排障

**屏上没反应 / 画面不动** —— 99% 是 DRM master 被面板服务抢走了。
立刻量这个计数器（这是唯一能证明"帧到底有没有推到玻璃上"的东西）：

```sh
cat /sys/class/spi_master/spi0/statistics/bytes
```

- **数字在涨** ⇒ 内核在推像素，问题在上层（渲染 / 版式）
- **数字不动** ⇒ 看 `/sys/kernel/debug/dri/0/clients` 里谁 `master y`
  （`doom` / `ucode` / `xwpmplay` 三者之一）

⚠️ 不要去看 `/dev/fb0` —— 它恒为全 0，是**假信号**。
⚠️ 一次全帧提交 = **+153,611 字节**，用它反算真实上屏帧数。

**★ 最坑的判据陷阱：面板空转恰好也是每秒 +153,611 B（1 fps）。**
所以「SPI 在涨」**不足以**证明 Doom 在跑。要这样看：

| SPI 增长速度 | 含义 |
|---|---|
| 约 **+153,611 B/s**（1 fps） | 面板 ucode 在空转 —— Doom 根本没拿到 master |
| 约 **+5,530,000 B/s**（36 fps） | Doom 在正常上屏 |

一眼判据：`cat /sys/kernel/debug/dri/0/clients`，看 `xwrt-doom` 那行的
`master` 列是不是 `y`。

**★ 面板服务为什么会自己回来（两个机制，缺一不可）**

```sh
/etc/init.d/xwrt-panel stop     # ← 必须！注销 procd 实例
for p in $(pgrep -f 'xwrt-panel/panel.uc'); do kill -9 $p; done
```

1. `xwrt-panel` 自带 `procd_set_param respawn`（`retry=5`）——**只要 procd 里那个
   实例还在，杀掉 ucode 5 秒后必被拉回来**。`disable` 只删 `/etc/rc.d` 软链，
   **不注销**已注册的实例，单独用它没用。
2. `/etc/init.d/xwrt-media` 的收尾动作会顺手 `/etc/init.d/xwrt-panel start`。

而且 `panel.uc` 在 procd 的 `term_timeout`（5 s）内不退，procd 日志里那句
`not stopped on SIGTERM, sending SIGKILL instead` 就是它 —— 所以手工清场一律
`kill -9`。

init 脚本里的 `panel_stop()` 做的正是「停服务 → 杀进程 → 轮询
`dri/0/clients` 确认 master 真空出来」，起 Doom 前会等它返回。

**★ 别用 `pgrep -x xwrt-doom` 认进程（BusyBox 的坑）**

BusyBox 的 procps 会把 `/bin/sh /etc/rc.common /etc/init.d/<脚本> <动作>`
这种进程的 comm **报成脚本的 basename**，于是 `/etc/init.d/xwrt-doom start`
自己就"叫 xwrt-doom"：

```
$ pgrep -x xwrt-doom            # 真机上实测
32175                            # ← 这是 init 脚本自己！
  comm=[xwrt-doom]  cmd=[/bin/sh /etc/rc.common /tmp/xwrt-doom start]
```

后果有两个，第二个是致命的：

1. `xwrt-doom status` 里 `running=` 永远是 1 —— **状态文件长期说谎**；
2. init 里的 `kill_doom()` 会 **SIGTERM 打中正在跑 `start_service` 的那个 shell**，
   `procd_open_instance` 根本没执行 ⇒ 症状是「服务说启动了、屏幕上什么都没有」，
   而且日志里连一行 `start` 都不打。

正确写法是**锚定绝对路径**（`^` 锚住 cmdline 开头，任何 `sh -c "…"` /
rc.common 包装层都不会误命中）：

```sh
pgrep -f '^/usr/bin/xwrt-doom'
```

本包三处（init 的 `doom_pids()`、`screen-guard.sh`、以及 doom 自己在
`DG_Init` 里写的 `/var/run/xwrt-doom.pid`）都是这么做的。

**日志**：`logread | grep xwrt-doom`，以及 procd 直接抓走的 stdout/stderr
（`logread` 里能看到引擎自己的 `==== 提交 N 帧 / T 秒` 收尾统计）。

**触摸坐标对不对**：手动前台跑一次，边跑边点，看事件回显。

```sh
/etc/init.d/xwrt-doom stop
xwrt-doom --wad /usr/share/xwrt-doom/doom1.wad --fps 25 --touch-echo
```

会打出每个事件：

```
[tin] DOWN FIRE    raw=( 240, 200) scr=(240,200) down=1
[tin] up   FIRE    raw=( 240, 200) scr=(240,200) down=0
```

`scr=` 就是屏幕坐标，应该和手指位置一致。GL-BE14000 上 `raw` 与 `scr` 相同
（DTS 的 swapped/inverted 与驱动内部的 RAW X 反相两层抵消），所以
`swap_xy` / `invert_x` / `invert_y` 都应该是 0。

**换了机型**：坐标对不上时先试这三个开关，再考虑改 DTS。

## 6. 开发者信息

引擎是 [doomgeneric](https://github.com/ozkl/doomgeneric)（GPL-2.0），
pin 在 `dcb7a8dbc7a16ce3dda29382ac9aae9d77d21284`。设备相关的部分全在本仓库
`package/xwrt-doom/src/`：

| 文件 | 职责 |
|---|---|
| `doom_be14000.c` | `main()` + 6 个 `DG_*` 钩子 + 动作→Doom 键码翻译 |
| `panel_out.c` | DRM 双 dumb buffer + 交替 `drmModeSetPlane` |
| `tinput.c` | evdev 单点 → 虚拟摇杆 + 锁定按钮 |
| `ovl.c` / `ovl.h` | 控制条版式、绘制、命中判定 |
| `actions.h` | `ACT_*` 中立动作层 |
| `font5x7.h` | 5x7 点阵字库 |

⚠️ **`D_DoomLoop()` 不是死循环**：它只做一次"起搏"就返回，无限循环必须由
平台层的 `main()` 自己驱动。漏掉它的症状极具迷惑性 —— 一切初始化正常、
第一帧也上了屏，然后进程以**退出码 0 干净退出**。

⚠️ 平台层**不能用 `/dev/fb0`**：fbdev 的刷新是异步且会被内核合并的，
连写 20 帧只出 0~3 帧，定不了帧率。

⚠️ `actions.h` 的存在理由：`<linux/input-event-codes.h>` 与 `doomkeys.h` 的
`KEY_*` 大面积重名（`KEY_TAB` / `KEY_ENTER` / `KEY_F1..F12` …），
混在一个编译单元里必炸。
